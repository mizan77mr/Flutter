import 'package:flutter/material.dart';

class SumitedPage extends StatelessWidget {
  const SumitedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Submited",style: TextStyle(fontSize: 40,color: Colors.white),),),
    );
  }
}
